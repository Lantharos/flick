#!/usr/bin/env node
"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
const runner_1 = require("./runner");
const [, , command, file] = process.argv;
// Show help if no arguments or wrong command
if (!command || command === "--help" || command === "help") {
    console.log(`
Flick - The programmer's language

Usage:
  flick run <file.fk>     Run a Flick source file
  flick --help            Show this help

Examples:
  flick run test.fk
`);
    process.exit(0);
}
// Only allow "run"
if (command === "run" && file) {
    console.log(`Running Flick file: ${file}`);
    (0, runner_1.runFlick)(file);
}
else {
    console.log(`
Error: Unknown usage.

Usage:
  flick run <file.fk>
  flick --help
`);
    process.exit(1);
}
//# sourceMappingURL=cli.js.map